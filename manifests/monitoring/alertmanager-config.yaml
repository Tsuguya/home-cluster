apiVersion: monitoring.coreos.com/v1alpha1
kind: AlertmanagerConfig
metadata:
  name: discord
  namespace: monitoring
spec:
  route:
    receiver: discord
    groupBy: ['namespace', 'alertname']
    groupWait: 30s
    groupInterval: 5m
    repeatInterval: 4h
    routes:
      - receiver: "null"
        matchers:
          - name: alertname
            value: InfoInhibitor
            matchType: "="
      - receiver: discord-low
        matchers:
          - name: alertname
            value: Watchdog
            matchType: "="
      - receiver: alert-investigate
        matchers:
          - name: severity
            value: critical
            matchType: "="
        continue: true
      - receiver: discord-critical
        matchers:
          - name: severity
            value: critical
            matchType: "="
        continue: true
      - receiver: discord
        continue: true
  inhibitRules:
    - sourceMatch:
        - name: severity
          value: critical
          matchType: "="
      targetMatch:
        - name: severity
          value: warning
          matchType: "="
      equal: ['namespace', 'alertname']
    - sourceMatch:
        - name: alertname
          value: InfoInhibitor
          matchType: "="
      targetMatch:
        - name: severity
          value: info
          matchType: "="
      equal: ['namespace']
  receivers:
    - name: "null"
    - name: discord
      discordConfigs:
        - apiURL:
            name: discord-webhook-alertmanager
            key: url
          sendResolved: true
    - name: discord-critical
      discordConfigs:
        - apiURL:
            name: discord-webhook-alertmanager-critical
            key: url
          sendResolved: true
    - name: discord-low
      discordConfigs:
        - apiURL:
            name: discord-webhook-alertmanager-low
            key: url
          sendResolved: true
    - name: alert-investigate
      webhookConfigs:
        - url: http://alertmanager-webhook-eventsource-svc.argo:12001/alertmanager
          sendResolved: false
