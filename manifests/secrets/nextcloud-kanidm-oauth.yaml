apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: nextcloud-kanidm-oauth
  namespace: nextcloud
spec:
  refreshInterval: 12h
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword
  target:
    name: nextcloud-kanidm-oauth
    creationPolicy: Owner
    deletionPolicy: Retain
  dataFrom:
    - extract:
        conversionStrategy: Default
        decodingStrategy: None
        key: kanidm-nextcloud-oauth
        metadataPolicy: None
