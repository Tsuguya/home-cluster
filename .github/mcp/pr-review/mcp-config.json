{
  "mcpServers": {
    "pr-review": {
      "command": "npx",
      "args": ["tsx", "src/index.ts"],
      "cwd": ".github/mcp/pr-review",
      "env": {
        "GITHUB_TOKEN": "${GITHUB_TOKEN}"
      }
    }
  }
}
