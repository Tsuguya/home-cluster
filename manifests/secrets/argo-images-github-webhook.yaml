apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: images-github-webhook
  namespace: argo
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword
  target:
    name: images-github-webhook
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: images-github-webhook
